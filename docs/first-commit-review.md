# 首次提交检查

核对日期：2026-09-19。仓库尚无提交。本次准备文件和修正文档，没有创建提交或加入暂存区。

## 建议提交的范围

| 范围 | 理由 |
| --- | --- |
| `boxagent/` | 当前产品源码：桌宠宿主、可替换形象、语音、通用任务执行、本地窗口摘要 |
| `scripts/`、`tests/` | uv 入口与锁文件、环境辅助脚本、自动检查、可复跑预实验；用途与副作用已在脚本目录说明 |
| `assets/pet/` | 公开图集、角色配置、协议、来源、MIT 许可及素材校验结果 |
| `README.md`、`docs/` | 当前使用方式、环境准备、实现说明，以及明确标为历史的规划和实验记录 |
| `.gitignore`、`.env.local.example` | 私有输出隔离规则与无真实密钥的配置示例 |
| `启动桌宠.command` | 相对项目目录启动的终端入口，已检查可执行权限 |
| `.codex/environments/environment.toml` | 仅包含相对启动命令的项目 Run 配置，无个人路径或凭据，可以提交 |

保留历史实验脚本用于解释技术选择和复现失败路径，不将它们作为产品运行依赖。第一版尚未指定项目代码的开源许可证；图集的 MIT 许可仅覆盖相应上游素材，不自动给本项目代码授予许可。本次没有代替作者选择许可证。

## 不提交的内容

真实 `.env` / `.env.*`、个人截图、界面文本、音频、运行日志、模型、Python 环境、下载的官方运行时、缓存与构建输出。`.env.local.example` 显式保留；`docs/assets/`、`artifacts/`、`logs/`、`.runtime/`、`results/`、`models/` 等均忽略。新增了 `.DS_Store` 与常见检查缓存忽略规则。

候选文件静态扫描没有发现个人绝对路径或疑似真实密钥；唯一二进制候选是公开 `spritesheet.webp`，其 SHA-256 与素材校验记录一致。此检查针对 Git 正常候选文件，不意味着任意私有目录可以安全强制加入 Git。

## 本次修整

- 补齐 15 秒／960 像素观察、约 2 秒打字、全文滚动、默认自动授权与结束通知的当前说明。
- 历史记录显式标记日期和适用范围，消除“未接入读屏”“撤下通知”“计算器限定是当前执行器”等误读。
- 增加新机器准备说明，明确模型、官方二进制、登录和系统权限不随仓库提供。
- 修复真实验收脚本的授权模式回归；音频输入改为显式路径；原生验收失败不再被应用正常退出状态掩盖。
- 检查 Markdown 本地文件链接、Python / Shell / Node 语法、公开图集哈希、忽略规则，以及测试输出目录隔离。

## 已验证与保留边界

本轮 33 项自动检查、26 项原生 UI 检查通过；另用故意失败的本地替身确认原生验收以退出码 1 报告故障。最终候选共 61 个文件，约 2.53 MiB，暂存区为空。没有重新运行真实云端语音或 Computer Use 操作。真实系统历史结果与当前未验证项见 [验收记录](poc-verification.md)。

目前适合提交为本机 POC 的初始版本，不能当作已完成可分发产品。主要边界：全新机器部署未验收；MLX 环境没有完整锁文件、模型下载未固定 revision；当前视觉参数未新增真实质量验收；光标自动清理和多小时运行仍待验证。关闭宿主的总等待时间与任务清理各自限时也需在长时运行测试中核查。

建议首次提交说明：`feat: 初始化 BoxAgent 桌宠 POC`。实际提交时使用约定的临时 Codex author / committer 身份，不修改仓库默认身份。
